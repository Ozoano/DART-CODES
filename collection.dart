//A group of values that belong together.... lists, sets, maps
// 1. list

void main() {
  var names = ['Uchenna', 'Ada', 'Chioma', 'Aham'];
  // for (var name in names) print(name);

  // print("");
  // for (var i = 0; i < names.length; i++) print(names[i]);

  print(names.insert(1, 'uchenna'));
}


/* Common list oppression
* names.length
* names.isEmpty
* names.isNptEmpty
* name.add()
* name.insert(pos, value)
* names.removeAt
* names.clear
* names.clean
*names.indexOf(one argv)
* names.Constant(one argv
*/